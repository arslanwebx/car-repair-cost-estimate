import { describe,expect,it } from "vitest";import { mergePhotoSelection,type PhotoLike } from "./photos";
const photo=(name:string,overrides:Partial<PhotoLike>={}):PhotoLike=>({name,size:1000,type:"image/jpeg",lastModified:1,...overrides});
describe("photo selection",()=>{it("appends new photos without replacing existing files",()=>{const first=photo("first.jpg"),second=photo("second.jpg");expect(mergePhotoSelection([first],[second]).files).toEqual([first,second])});it("rejects duplicates and retains the original",()=>{const first=photo("same.jpg");const result=mergePhotoSelection([first],[first]);expect(result.files).toEqual([first]);expect(result.errors[0]).toContain("already selected")});it("enforces the ten-photo maximum",()=>{const existing=Array.from({length:10},(_,index)=>photo(`${index}.jpg`,{lastModified:index}));const result=mergePhotoSelection(existing,[photo("extra.jpg")]);expect(result.files).toHaveLength(10);expect(result.errors[0]).toContain("maximum")});it("rejects unsupported or oversized files",()=>{const result=mergePhotoSelection([], [photo("bad.gif",{type:"image/gif"}),photo("huge.jpg",{size:11*1024*1024})]);expect(result.files).toHaveLength(0);expect(result.errors).toHaveLength(2)})});
