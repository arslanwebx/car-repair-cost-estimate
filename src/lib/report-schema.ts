import { z } from "zod";
import { estimateInputSchema, visionAnalysisSchema } from "./estimate-schema";
const moneyRangeSchema=z.object({low:z.number().nonnegative(),high:z.number().nonnegative()}).refine(value=>value.high>=value.low);
const partsCategorySchema=z.enum(["economical","aftermarket","recycled_oem","new_oem"]);
export const pricedEstimateSchema=z.object({pricingVersion:z.string().min(1),market:z.string().min(1),mid:z.number().nonnegative(),selectedPartsCategory:z.enum(["economical","aftermarket","recycled_oem","new_oem","all"]),items:z.array(z.object({area:z.string().min(1),operation:z.string().min(1),operationCode:z.string().min(1),laborHours:moneyRangeSchema,parts:moneyRangeSchema,bodyLabor:moneyRangeSchema,paint:moneyRangeSchema,frameLabor:moneyRangeSchema,mechanicalLabor:moneyRangeSchema,consumables:moneyRangeSchema})).min(1),partsScenarios:z.array(z.object({category:partsCategorySchema,label:z.string().min(1),parts:moneyRangeSchema,total:moneyRangeSchema,oemFallbackItems:z.number().int().nonnegative()})).min(1),scanCalibration:moneyRangeSchema,shopSupplies:moneyRangeSchema,hiddenDamage:moneyRangeSchema,subtotal:moneyRangeSchema,tax:moneyRangeSchema,total:moneyRangeSchema});
export const reportSchema=z.object({id:z.string().min(5).max(80),generatedAt:z.string().datetime(),input:estimateInputSchema,vision:visionAnalysisSchema,estimate:pricedEstimateSchema});
export type EstimateReport=z.infer<typeof reportSchema>;
