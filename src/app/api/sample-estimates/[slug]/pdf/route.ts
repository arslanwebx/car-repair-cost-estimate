import { NextResponse } from "next/server";
import { getSampleEstimate } from "@/data/sample-estimates";
import { createSamplePdf } from "@/lib/sample-pdf";
import { withConcurrencyLimit, WorkloadBusyError } from "@/lib/concurrency";
import { sanitizeImage } from "@/lib/image-processing";
export const runtime="nodejs";
export async function GET(request:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;const sample=getSampleEstimate(slug);if(!sample)return NextResponse.json({error:"Sample estimate not found."},{status:404});try{return await withConcurrencyLimit("pdf-generation",1,async()=>{const response=await fetch(new URL(sample.image,request.url),{signal:AbortSignal.any([request.signal,AbortSignal.timeout(10_000)])});if(!response.ok)throw new Error("Sample image is unavailable.");const file=await sanitizeImage(await response.arrayBuffer(),{width:1400,height:900,quality:84,timeoutSeconds:20});if(request.signal.aborted)throw request.signal.reason;const pdf=await createSamplePdf(sample,`data:image/jpeg;base64,${Buffer.from(file).toString("base64")}`);return new NextResponse(new Uint8Array(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="carspect-${slug}.pdf"`,"Cache-Control":"public, max-age=3600, stale-while-revalidate=86400"}})})}catch(error){if(error instanceof WorkloadBusyError)return NextResponse.json({error:"PDF generation is busy. Please wait a moment and try again."},{status:503,headers:{"Retry-After":"5"}});console.error("Carspect sample PDF generation failed",{slug,type:error instanceof Error?error.name:"UnknownError",message:error instanceof Error?error.message:"Unknown failure"});return NextResponse.json({error:"The sample PDF could not be generated. Please try again."},{status:500})}}
