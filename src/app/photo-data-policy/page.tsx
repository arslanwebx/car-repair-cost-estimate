import type { Metadata } from "next"; import { ContentPage } from "@/components/content-page"; export const metadata:Metadata={title:"Photo and Data Handling Policy",alternates:{canonical:"/photo-data-policy"}};
export default function Page(){return <ContentPage title="Photo and Data Handling Policy" intro="Here is what happens to an image when you ask Carspect to analyze vehicle damage."><h2>Upload and sanitization</h2><p>The server checks file count, size, declared type, and whether image software can decode the file. Accepted JPG, PNG, and WebP images are rotated, resized when needed, and re-encoded as JPEG. This removes embedded EXIF metadata, including location data, before AI processing.</p><h2>AI processing and storage</h2><p>Sanitized images and relevant vehicle and damage details are sent to the AI provider configured by the site operator. The current implementation processes images in memory and does not intentionally save them to a Carspect database or storage bucket. Images are not placed in public storage and are not used for model training without separate, explicit, optional consent.</p><h2>Deletion and private links</h2><p>Because this release does not persist estimates or photos, there is no stored Carspect copy to delete after the request completes. Private report links are not currently implemented, so the interface does not offer them. If storage or sharing is added, signed expiring links, manual deletion, automatic retention enforcement, and updated disclosures are required before release.</p><h2>Questions</h2><p>For a privacy or removal request, contact support@carspect.pro and provide enough non-sensitive context for us to locate the request. Never email vehicle photos or a full VIN unless support specifically asks for a secure submission method.</p></ContentPage>}
