import type { MetadataRoute } from "next";import { sampleEstimates } from "@/data/sample-estimates";export default function sitemap():MetadataRoute.Sitemap{const base="https://carspect.pro",paths=["","/estimate","/sample-estimates",...sampleEstimates.map(sample=>`/sample-estimates/${sample.slug}`),"/about-us","/contact-us","/privacy-policy","/terms-of-service","/disclaimer","/cookie-policy","/photo-data-policy","/editorial-policy"];return paths.map(path=>({url:base+path,lastModified:new Date("2026-07-11"),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:path==="/estimate"?.9:.6}))}
